import type { CapacitorConfig } from '@capacitor/core';

const config: CapacitorConfig = {
  appId: 'agency.luminari.tico',
  appName: 'Tico',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
